# AI development log

| Tool | What I asked / generated | What I changed / decided myself |
|------|--------------------------|----------------------------------|
| Cursor (Composer) | Full project scaffold: `src/` layout, `package.json` scripts, `tsconfig.json`, ESLint config, empty typed route/middleware stubs, and `.env.example`. | Kept the structure as generated. Restored the spec lint script `eslint 'src/**/*.ts'` for CI (Ubuntu); it fails in PowerShell locally so I run `npx eslint "src/**/*.ts"` on Windows during development. |
| Cursor (Composer) | PostgreSQL pool in `src/db/index.ts`, `runMigrations()` with `links` and `click_events` tables, indexes, and startup migration call in `index.ts`. | Accepted the schema mostly as-is. Switched `DATABASE_URL` in `.env.example` to use the `postgresql://` scheme and Docker hostname `db` once Docker Compose was added. Fixed a startup ordering issue by loading dotenv via `import 'dotenv/config'` in the db module so the pool sees env vars before init. |
| Cursor (Composer) | All four core endpoints: POST/GET/DELETE `/api/links`, GET `/:slug` redirect with click_count increment and `click_events` insert. | Refactored route handlers from inline `try/catch` to `.catch(next)` after adding the global error handler. Moved POST validation out of the route into `validateCreateLink` middleware and wired `createLink` separately in `app.ts` so rate limiting and validation apply only to POST. Chose to return plain `{ error: "..." }` JSON on redirect 404s instead of the nested AppError shape used elsewhere. |
| Cursor (Composer) | `validateCreateLink` middleware (URL constructor check, slug regex, future `expires_at`) and global `errorHandler` for PG codes `23505`/`23503`. | Removed the old generic `validate()` / `requireBody()` helpers in favour of the single explicit middleware. Dropped `AppError` entirely once the error handler was simplified to PG-specific 409s and a generic 500. Aligned slug validation to 3–20 chars to match `VARCHAR(20)` and restricted URLs to http/https only. |
| Cursor (Composer) | Multi-stage `Dockerfile`, `docker-compose.yml` with Postgres 16, `.dockerignore`, and Docker-oriented `.env.example` values. | Fixed the db healthcheck after seeing repeated `FATAL: database "user" does not exist` in Docker Desktop logs — `pg_isready -U user` defaults to a database named after the user, so I added `-d ${POSTGRES_DB}` to target `url_shortener`. Verified the stack with `docker compose up --build -d` and confirmed `/health` returns `db: connected`. |
| Cursor (Composer) | Integration tests in `test/links.test.ts` with `jest.mock('../src/db')`, 13 cases covering all endpoints, `jest.config.ts`, and `.github/workflows/ci.yml`. | Adjusted the GET `/api/links` assertion to expect ISO date strings (JSON serialization) rather than `Date` objects. Used `../src/db` as the mock path from the `test/` directory instead of the illustrative `../db` from the prompt. Suppressed `console.error` noise in tests when exercising the 409 error path. |
| Cursor (Composer) | Integration tests with jest.mock, 13 cases across all endpoints, jest.config.ts, and CI workflow. | Noticed generated tests only asserted status codes on error paths, added body assertions for all 400/404/409 responses to verify the error message shape, not just the HTTP status. Adjusted mock path to `../src/db` from the `test/` directory. Suppressed `console.error` noise on the 409 path. |
| Cursor (Composer) | Full README structure including setup steps, API reference table, and curl examples. | Rewrote all curl examples by hand after running each one against the live container, two had incorrect Content-Type headers. Verified every response shape matched the actual API output before committing. |
